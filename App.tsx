import React from 'react';

import { MainScreen } from './src/components/MainScreen';

export default function App() {
  return (
    <MainScreen />
  );
}

// 1'x' = (P'x') / (P'y') в 'y' валюту
// Т.е 1$ = 50P и 1Г = 2Р, то 1$ в гривнах = 50/25 = 2 хривны (цена джинс)